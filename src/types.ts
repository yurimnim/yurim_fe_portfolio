export type ProjectContents = {
    imageList?: string[];
    placeholderLabel?: string;
    title: string;
    subtitle: string;
    description: string;
    techStack?: string[];
    url?: string[];
};
